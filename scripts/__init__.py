"""adversarial-plan scripts package.

Exists so the orchestrator can be imported as ``scripts.adversarial_plan``
and the phase modules as ``scripts.phases.*`` (mirrors adversarial-spec,
which itself mirrors adversarial-code-loop).
"""
